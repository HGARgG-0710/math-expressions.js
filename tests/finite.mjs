// * Tests for the 'finite' method

// ! Add the tests for the 'finite' method of the 'native.mjs'; 
// ? Maybe, just use the tests that are ALREADY inside the library for it? [ex: integer.native, algarray.native... after having fixed the missing templates and in-out sequence-arguments...]; 