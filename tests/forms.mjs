// * Tests related to forms

import {
	form,
	constForm,
	propertyForm,
	objectForm,
	classForm,
	arrayForm,
	garrayForm,
	treeForm
} from "../src/modules/exports/structure.mjs"

// ! List [from "structure.mjs"]:
//  structure.form - construct a form, print it out; print out all the kinds of ready library forms;
// ! test every method against all present forms (meaning - create an instance of the form, put in the instance + value of the form into the method, run)
//  structure.structure - test for each case two or three times with differing templates;

// % FORMS (to be printed out and tested with 'structure'):
//  structure.constForm
//  structure.propertyForm
//  structure.objectForm
//  structure.arrayForm
//  structure.classForm
//  structure.garrayForm
//  structure.treeForm
