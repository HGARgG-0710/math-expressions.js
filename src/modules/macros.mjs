// * Space for macros and local constants... [used for semantics and simplification of development/code-reading];

// ? (old) TODO [general]: make a total safe-check for ALL the methods/macros/functions/classes regarding anything concerning parameter values [default values, the transformations used, alternative values and the way that they behave collectively...];

export {
	ID,
	TYPED_VARIABLE,
	VARIABLE,
	RECURSIVE_VARIABLE,
	NAMED_TEMPLATE,
	READONLY,
	TEMPLATE,
	// INHERIT,
	EXTENSION,
	NOREST,
	PRECLASS,
	CLASS,
	DEOBJECT,
	OBJECT,
	NOMODULE, 
	FUNCTION
} from "../lib.mjs"
